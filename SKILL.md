---
name: jetbrains-idea-workflow
description: '在 JetBrains IDEA 当前项目中使用 IDEA MCP 完成源码理解、需求分析、系统设计、前后端功能开发、Bug 修复、重构、数据库与界面变更、检查和验证。用于任务依赖当前 IDEA 项目的源码、符号、inspection、运行配置、依赖或数据库语义，并需遵循项目规范完成证据、实施和验证闭环。'
---

# JetBrains IDEA 开发工作流

按用户明确要求的任务类型完成分析、设计、修改、验证和交付。输出、说明、确认、设计及总结均使用中文。

## 核心规则

### 硬不变量

- 不泄露敏感数据、密钥、令牌或连接凭据。
- 涉及项目语义时，以当前 IDEA 项目的源码、配置、依赖、检查结果和数据库只读信息为证据。
- 数据库写入按 [数据库SQL与持久化层.md](references/数据库SQL与持久化层.md)
  取得精确确认；其他高风险操作按 [高风险确认.md](references/高风险确认.md) 独立确认。
- 不覆盖、回退、删除或格式化用户已有的无关改动。
- 不通过抑制检查、删除测试、弱化断言、降低类型安全或吞异常掩盖问题。
- 结果存在失败、超时、未分析、截断、取消、缺少终态或部分批次时，不得表述为通过。
- 修改项目或 skill 文件以及数据库数据或结构前，必须完成“修改前强制方案门禁”，输出适用的当前完整方案并取得用户明确确认和实施授权；用户在方案展示前提出的修改、修复、实现或执行要求不能替代该门禁。

### 优先级

不与硬不变量冲突时，依次服从用户当前明确指令及范围、项目 `AGENTS.md`、本 skill、项目既有约定和一般工程习惯。

用户指令只授权其中能够唯一确定的操作、对象和范围。数据库写入及其他明确要求独立确认的操作，继续按对应 reference 取得确认。

### 问题审查与证据表达

处理任务前，审查其前提、推理和信息是否足以支持目标。只展开会实质影响当前结论、范围、行为或风险的事项。

- 前提与证据冲突，或存在因果倒置、条件遗漏、范围混淆和结论跳跃时，指出具体问题、依据和影响；能够按纠正后的前提继续时直接继续。
- 对影响结论且能在当前范围内查明的内容，从项目规范、当前源码、配置、依赖、数据库只读信息、项目文档或适用的权威原始资料核实。
- 在可能混淆时区分已核实事实、基于证据的推断和主观取舍；无法核实时标明未验证，不虚构来源。
- 只指出足以改变结论的遗漏变量、兼容代价、实施维护成本、性能和验证成本，不展开与当前任务无关的通用风险清单。

### 任务类型

一个请求可以同时属于多种任务类型；分别应用每种类型的规则。

- 分析型：阅读、定位、解释、诊断、评审或影响分析，不编辑项目文件。
- 方案型：进行方案比选或输出确定性方案正文，不实施方案。
- 修改型：实现、修复、重构或修改代码、页面、SQL、配置、测试、文档、skill 及数据库内容；先完成修改前强制方案门禁，取得当前方案确认和实施授权后才进入编辑检查闭环。
- 验证型：只执行指定或已获授权的检查、构建、测试或运行；请求不包含修复时，失败后只报告。
- 元任务：维护本 skill 或处理不依赖 IDEA 项目语义的普通文档。元任务只决定工具边界，不排斥其同时属于分析型、方案型、修改型或验证型。

意图能由请求和上下文唯一确定时直接继续；只有不同理解会实质改变范围、行为或结果时才询问。

### 修改前强制方案门禁

#### 任务入口判定

开始任务时，先判断任务是否可能产生内容修改：

1. 只读分析、解释、诊断、评审以及不包含修复的验证，不生成修改方案。
2. 只执行不修改项目内容的构建、测试、运行或只读 Git 查询时，不生成三种修改方案正文，按非文件级执行规则判断授权。Git
   commit、Git push、部署以及其他会修改工作区、索引、版本历史或外部环境的操作，不生成三种修改方案正文，但必须按高风险规则判断并取得适用的独立确认。
3. 可能修改代码、页面、接口、SQL、配置、测试、文档、规范、skill、数据库数据或结构时，必须在首次修改前完成本节规定的方案和确认流程。

用户最初提出“修改”“修复”“实现”“开始修改”或同等要求，只确定任务目标，不替代方案正文确认和实施授权。

#### 正文类型判定

用户明确指定系统功能详细设计、工程开发计划、文档修改方案或对应格式文件时，使用其指定类型。指定类型与任务目标冲突，导致该格式不能完整表达所需结果时，说明冲突、依据和影响并取得确认，不自行改用其他类型。

用户未指定正文类型时，按以下顺序判定：

1. 实施对象是文档、规范、说明性配置或 skill 内容，且正文需要说明这些内容如何修改时，使用 `文档修改方案格式.md`
   。系统功能设计或工程开发计划即使以文档形式交付，也不因此归入文档修改方案；改变应用、构建、部署或运行行为的可执行配置按工程开发计划判定。
2. 不属于第一项时，先完成必要的只读查证，判断目标系统行为和验收边界是否已经确定。目标系统行为包括业务规则、主流程、关键分支、状态转换、使用主体、角色权限、输入、输出、异常和用户反馈。
3. 上述行为在必要查证后仍不能唯一确定，且当前任务需要对其作出设计时，先解决待定方向，再使用 `系统功能详细设计格式.md`
   输出已经确定的最终设计，回答“系统应该做什么”。仅因尚未读取源码、配置、文档或数据库，或者仅存在不改变目标系统行为的技术实现选择，不得使用该类型。
4. 目标系统行为和验收边界已经由用户指令、已确认设计或项目证据唯一确定，任务需要修改代码、页面、接口、数据、SQL、可执行配置或测试时，使用
   `工程开发计划格式.md`，回答“工程上如何实现”。本 skill 中的工程开发计划用于规定代码、页面、接口、数据、SQL、可执行配置和测试的实施内容。行为已经确定的新功能、按钮和交互，以及
   Bug 修复、重构、性能优化、依赖调整和不改变既定功能行为的技术改造，均按本项判定。
5. 工程实现存在多个技术方向，但这些方向不改变目标系统行为时，仍属于工程开发计划；不得因为技术实现尚未选择而改用系统功能详细设计。

#### 方案形成

1. 完成适用的源码、配置、文档和数据库只读查证。
2. 按“事实、决策与授权”解决所有影响方案的待定事项。
3. 确有多个技术上可实施、符合用户约束且有实质差异的方向时，只给出带稳定编号或名称的候选摘要、主要差异、影响和风险，等待用户选择。
4. 用户补充或修改意见时更新候选项并继续等待；只有回复能够唯一确定方向后才进入详细正文。
5. 方向唯一或已经选定后，按“正文类型判定”加载对应格式文件并输出当前完整正文。
6. 正文只能使用已经查明的事实和已经确认的方向，不得保留候选项、推荐项、假设、占位文字或推迟到实施阶段决定的内容。

不得为了提供选项而虚构方向。候选数量由实际可行方向决定；只有一个可行方向时直接进入详细正文。不得列出明显无法实施、违反用户约束或项目事实、需要未确认范围扩张才能成立，或者与其他候选实质相同的方向。

候选阶段只说明足以支持选择的方向名称、核心路径、主要差异、影响和风险，不提供逐文件修改内容、核心实现逻辑、界面图、业务流程图或完整正文。

#### 方案确认与实施状态

- 候选方向选择只确认方向，不确认详细正文，也不授权任何修改。
- 文档修改方案输出后停止。只有用户已经看到当前完整正文，并明确确认该正文且要求按其实施，才可以修改文档、规范、说明性配置或
  skill 内容。
- 目标系统行为已经确定时，工程开发计划输出后停止。只有用户已经看到当前完整计划，并明确确认该计划且要求按其实施，才可以修改代码、页面、接口、SQL、可执行配置、测试或者数据库内容。
- 目标系统行为尚未完成设计时，先输出系统功能详细设计并停止。该设计的确认只确认系统行为，不授权输出工程开发计划，也不授权代码修改。
- 系统功能详细设计确认后，用户同一回复已经明确要求继续输出工程开发计划时，直接输出工程开发计划；否则询问用户是否继续输出工程开发计划，并在询问后停止。
- 用户同意继续只授权输出工程开发计划，不授权代码修改。工程开发计划输出后，必须再次等待该计划的确认和实施授权。
- 系统功能详细设计不能代替工程开发计划。任何代码修改前都必须存在当前有效、已经确认且获得实施授权的工程开发计划。
- 用户明确表示只需要系统功能详细设计、不需要工程开发计划或实施时，设计确认后结束，不再询问是否继续。
- 用户在系统功能详细设计、工程开发计划或文档修改方案展示前提出的宽泛实施要求，不能跨越后续确认阶段。
- 用户在看到当前完整工程开发计划或文档修改方案后回复“按本计划开始修改”“执行当前计划”或其他能够唯一表达确认和实施授权的指令时，可以在一条回复中同时完成正文确认和实施授权。
- 仅回复“方案没问题”“设计没问题”等内容，只确认当前正文；没有明确实施要求时不得开始修改。
- 当前确认只覆盖计划中明确列出的目标、文件、符号、行为、接口、数据、配置、验证范围和排除范围。
- 同一确认可以覆盖当前计划范围内的多次小步编辑、格式化以及由本次变更直接引入且计划已经覆盖的修复。
- 出现新的文件、符号、业务行为、接口、数据、权限、风险或验收范围时，停止依赖新增范围的修改，更新完整计划并重新确认。
- 数据库实际写入继续要求精确 SQL、目标数据源、数据库、schema 和影响范围确认；依赖变更、环境变更、删除、Git、部署和其他高风险操作继续取得各自的独立确认。

#### 方案正文修订

- 用户继续修改已经输出的方案正文时，每次都重新给出吸收全部意见后的完整正文，不只列出本轮新增内容、删除内容、差异摘要或局部替换文字。
- 新版正文直接呈现最终状态，不记录修改过程，不保留差异说明，也不写成变更日志。
- 用户要求删除、取消或替换某项内容时，直接更新最终正文，不使用“已删除”“不再包含”“按用户要求调整”等表述重复呈现变化。
- 修订后的正文替代旧正文；旧正文的确认及其实施授权立即失效。
- 修订意见仍构成最终范围、排除范围、兼容要求或风险边界时，按与其他正式内容相同的层级和语气保留。

## 项目证据与工具边界

涉及项目源码、符号、依赖、工作区、VCS、编辑、inspection、运行配置、构建、数据库或调试能力时，使用 IDEA MCP 取得当前项目证据。

需要跨文件定位、调用关系或影响候选，且 CodeGraph 可用并适合当前查询时，使用 CodeGraph 辅助定位；关键结论仍由 IDEA MCP
读取的当前项目内容复核。已有证据足够、CodeGraph 不可用或无结果时，不继续尝试，也不自行初始化、重建或修复索引。

仅维护本 skill、处理普通文档或执行不依赖 IDEA 项目语义的元任务时，可以使用普通文件系统工具。

按任务确认项目根、模块和目标文件归属。目标受 VCS 管理时，修改前读取相关仓库的已暂存、未暂存、未跟踪及冲突状态；目标不受 VCS
管理时，不假设存在 Git 仓库。

- Java、Kotlin 及其他具有可靠语义模型的源码，优先使用符号、定义和调用分析。
- Vue、TypeScript、模板、样式和配置先按精确文件名、标识符、固定文本或正则定位，再读取当前内容。
- 前后端契约以 HTTP method 和 URL 串联 Controller、请求响应类型、API 封装和页面调用。
- 依赖、JDK、JAR、JRT 或反编译源码由 IDEA 读取并核对当前版本。
- 公共符号重命名使用语义重构，不用文本全局替换模拟。

IDEA MCP、CodeGraph 或任务必需工具不可用时，不伪造已经读取或验证的证据；说明缺失能力、受影响范围和仍能完成的部分。

## IDEA MCP 子工具命令

IDEA 子工具统一通过 `mcp__idea__execute_tool` 调用。已知任务项目时，外层传入其绝对 `projectPath`；子工具名称和参数放入
`command`。

同一对话中首次使用某类子工具前，完整读取对应契约；对应契约已经完整掌握且未变化时，按“标准闭环”的复用规则不重复读取：

- 搜索、读取、符号、目录、编辑器状态和 VCS：读 [IDEA-MCP上下文工具契约.md](references/IDEA-MCP上下文工具契约.md)。
- 创建文件、格式化、重命名、inspection 和构建：读 [IDEA-MCP编辑与验证工具契约.md](references/IDEA-MCP编辑与验证工具契约.md)。
- 数据源、schema、数据库对象、查询和结果：读 [IDEA-MCP数据库工具契约.md](references/IDEA-MCP数据库工具契约.md)。
- 运行配置及运行任务：读 [IDEA-MCP运行工具契约.md](references/IDEA-MCP运行工具契约.md)。

一项任务使用多类工具时，读取全部对应文件。四份契约列出本 skill 允许调用的 33
个子工具及静态确认的名称、参数和条件；不得使用清单外工具，也不得根据相邻工具推断名称和参数。统一入口的当前工具说明可以补充契约未规定的单位、合法值、路径基准和序列化细节，但不得新增清单外工具，也不得替代静态契约已经明确的工具名、参数名、类型、必填项或组合条件。两者发生冲突时，停止调用并报告契约漂移，不自行选择其中一方。

命令参数 `command`
按统一入口要求传入包含子工具名和参数的命令行字符串。只有对应静态契约、统一入口当前工具说明允许补充的内容，或同一工具、同一参数结构的有效既有命令能够唯一确定序列化方式时，才构造命令。可选参数的单位、合法值、路径基准或序列化规则未查明时，省略该参数；必填参数仍无法唯一构造时，报告契约缺口并停止，不试探写法。

构造和判读命令时：

1. 明确命令目的、目标对象和预期证据。
2. 从对应契约复制工具名、参数名、必填项、已确认类型和条件组合。
3. 核对外层项目、路径基准、字符串边界、数组、对象、布尔值、数字和互斥组合。
4. 同时检查统一入口和实际子工具结果；外层调用成功不代表子工具成功。
5. 输入、配置和外部状态未变化时，不为取得不同结果盲目重跑。

首次准备执行终端命令、脚本、包管理器或包装入口、编译、构建、测试，或者处理高输出、失败、超时和结果丢失时，读取 [命令执行可靠性.md](references/命令执行可靠性.md)
。成功的简单 IDEA 搜索、读取和状态查询不要求加载该文件。

## 任务路由

下列条件独立判断。同一任务命中多项时，加载全部命中项；标明“追加”的文件不得替代其基础文件。

- 源码理解、定位、调用与影响分析、Bug 排查或准备修改：读 [源码与上下文阅读.md](references/源码与上下文阅读.md)。
- 修改或新增项目文件，以及处理本次变更文件的格式化、IDEA inspection
  和文件级只读检查：读 [编辑格式化与文件级检查.md](references/编辑格式化与文件级检查.md)。
- 数据库、SQL、持久化层、ORM、Mapper、DAO、Repository、字段、字典、权限数据、数据异常、DDL 或
  DML：读 [数据库SQL与持久化层.md](references/数据库SQL与持久化层.md)。
- 任务可能修改代码、页面、接口、SQL、配置、测试、文档、规范、skill 内容或者数据库数据与结构：先执行“修改前强制方案门禁”。
- 判定为系统功能详细设计：读 [系统功能详细设计格式.md](references/系统功能详细设计格式.md)。
- 判定为工程开发计划：读 [工程开发计划格式.md](references/工程开发计划格式.md)。
- 判定为文档修改方案：读 [文档修改方案格式.md](references/文档修改方案格式.md)。
- 前端任务：读 [前端开发工作流.md](references/前端开发工作流.md)；Vue 页面、组件、指令、composable、store 或 Vue
  工具链任务追加 [Vue开发工作流.md](references/Vue开发工作流.md)
  ；新界面、视觉重构、样式美化或体验提升追加 [前端界面设计质量.md](references/前端界面设计质量.md)。
- 目录、模块、项目、工作区或运行环境级
  inspection、lint、类型检查、编译、构建、测试、启动、浏览器、截图、长任务、独立验证或运行依赖问题：读 [非文件级执行与验证.md](references/非文件级执行与验证.md)。
- 删除非数据库文件或版本历史、不可逆操作、依赖或环境变更、持久化 IDE 配置、部署、发布、Git commit、Git push 或
  CI/CD：读 [高风险确认.md](references/高风险确认.md)。

## 事实、决策与授权

- 技术事实：从项目规范、源码、配置、文档、依赖或数据库只读信息查明。
- 待定决策：必要查证后仍不能唯一确定，且不同选择会改变业务行为、范围、契约、数据、安全或结果。
- 操作授权：查明实际入口、工作目录、目标范围、展开行为和主要影响后，判断用户当前明确指令是否覆盖该操作。

发现待定决策或未解决授权时：

1. 继续完成解决该事项所需的最小只读查证，以及与该事项完全独立且不会影响其结果的工作。
2. 暂停依赖该事项的编辑、执行和验证。
3. 使用稳定的 `Q1`、`Q2` 编号说明已知事实、未知内容、影响和范围；适合时提供 A/B/C 与建议。
4. 提问后结束当前轮。

文件级默认授权和检查闭环由 [编辑格式化与文件级检查.md](references/编辑格式化与文件级检查.md)
负责；文件范围之外的执行由 [非文件级执行与验证.md](references/非文件级执行与验证.md)
负责；数据库写入由 [数据库SQL与持久化层.md](references/数据库SQL与持久化层.md)
负责；其他高风险操作由 [高风险确认.md](references/高风险确认.md) 负责。

## 确认状态

每项只处于未确认、已确认或已失效状态。回复能够唯一确定选择和范围时才确认；用户拒绝、取消相关工作或范围变化使事项不再影响后续时，记为已失效并记录原因。

- 单个、范围唯一的问题，可以用能够唯一对应的自然语言肯定或否定确认。
- 多个问题时，“继续”“开始”“可以”“你看着办”等泛化回复不覆盖未逐项对应的事项。
- 部分回复只更新对应事项；保留其他未确认项，不重复询问已确认项。
- 推荐、沉默和超时不是默认答案。
- 用户明确表示全部按已列出的推荐项及范围处理，且各项唯一清晰时，可以确认对应事项。
- 新回复改变选择、范围或产生分支时，以最新明确指令为准并重新检查受影响工作。

依赖某项确认的编辑、执行或验证，只能在该项已确认或已失效后进行；解决该项所需的最小只读查证不受此限制。

## 标准闭环

1. 审查任务前提、信息完整性和证据状态，确定任务类型、范围、风险及全部适用 reference。
2. 读取适用的 `AGENTS.md`、项目文档、当前工作区状态和任务上下文。
3. 使用任务所需的 IDEA MCP，并按需使用 CodeGraph，取得当前项目证据。
4. 核实事实、检验推断并查明实际影响。
5. 解决影响结果的待定决策和操作授权。
6. 任务可能产生内容修改时，执行“修改前强制方案门禁”，在每个规定的方案输出或确认节点停止；只读任务不生成修改方案。
7. 只有当前有效的工程开发计划或文档修改方案已经取得确认和实施授权后，才在其范围内小步编辑。
8. 按 [编辑格式化与文件级检查.md](references/编辑格式化与文件级检查.md) 完成本次变更文件的闭环，只修复当前计划已经覆盖且由本次变更直接引入的问题。
9. 按独立授权执行必要的非文件级验证、数据库写入和高风险操作。
10. 复核结果完整性，按真实终态交付。

同一对话复用已经完整掌握且未变化的 reference、项目规范、文件内容和分析结论；只在内容变化、首次读取失败或不完整、上下文未保留必要正文、证据冲突或用户明确要求时补读缺失范围。

## 结果终态

- 已完成：请求范围完成，所有强制验证取得完整结果。
- 已完成但未充分验证：实现完成，非强制验证因授权、环境或能力限制未完成；明确说明限制，不宣称通过。
- 待确认：已经输出候选方向、系统功能详细设计、是否继续输出工程开发计划的问题、工程开发计划或文档修改方案，正在等待用户完成当前阶段确认；不得表述为已完成或阻塞。
- 阻塞：待定事项、必需工具、强制验证或高风险授权未解决；说明阻塞并请求必要输入。

准确说明已完成内容、当前方案或确认阶段、验证范围、未验证项和阻塞。任何修改任务都不得绕过“修改前强制方案门禁”；系统功能详细设计、工程开发计划和文档修改方案分别按对应格式文件交付。
