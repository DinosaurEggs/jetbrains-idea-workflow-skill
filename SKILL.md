---
name: jetbrains-idea-workflow
description: '在 JetBrains IDEA 当前项目中使用 IDEA MCP 完成源码理解、需求分析、系统设计、前后端功能开发、Bug 修复、重构、数据库与界面变更、检查和验证。用于任务依赖当前 IDEA 项目的源码、符号、inspection、运行配置、依赖或数据库语义，并需遵循项目规范完成证据、实施和验证闭环。'
---

# JetBrains IDEA 开发工作流

按用户明确要求的任务类型完成分析、设计、修改、验证和交付。输出、说明、确认、设计及总结均使用中文。

## 核心规则

### 硬不变量

- 不泄露敏感数据、密钥、令牌或连接凭据。
- 涉及项目语义时，以当前 IDEA 项目的源码、配置、依赖、检查结果和数据库只读信息为证据。
- 数据库写入按 [数据库SQL与持久化层.md](references/数据库SQL与持久化层.md)
  取得精确确认；其他高风险操作按 [高风险确认.md](references/高风险确认.md) 独立确认。
- 不覆盖、回退、删除或格式化用户已有的无关改动。
- 不通过抑制检查、删除测试、弱化断言、降低类型安全或吞异常掩盖问题。
- 结果存在失败、超时、未分析、截断、取消、缺少终态或部分批次时，不得表述为通过。

### 优先级

不与硬不变量冲突时，依次服从用户当前明确指令及范围、项目 `AGENTS.md`、本 skill、项目既有约定和一般工程习惯。

用户指令只授权其中能够唯一确定的操作、对象和范围。数据库写入及其他明确要求独立确认的操作，继续按对应 reference 取得确认。

### 问题审查与证据表达

处理任务前，审查其前提、推理和信息是否足以支持目标。只展开会实质影响当前结论、范围、行为或风险的事项。

- 前提与证据冲突，或存在因果倒置、条件遗漏、范围混淆和结论跳跃时，指出具体问题、依据和影响；能够按纠正后的前提继续时直接继续。
- 对影响结论且能在当前范围内查明的内容，从项目规范、当前源码、配置、依赖、数据库只读信息、项目文档或适用的权威原始资料核实。
- 在可能混淆时区分已核实事实、基于证据的推断和主观取舍；无法核实时标明未验证，不虚构来源。
- 只指出足以改变结论的遗漏变量、兼容代价、实施维护成本、性能和验证成本，不展开与当前任务无关的通用风险清单。

### 任务类型

一个请求可以同时属于多种任务类型；分别应用每种类型的规则。

- 分析型：阅读、定位、解释、诊断、评审或影响分析，不编辑项目文件。
- 方案型：进行方案比选或输出确定性方案正文，不实施方案。
- 修改型：实现、修复、重构或修改代码、页面、SQL、配置及测试，进入编辑检查闭环。
- 验证型：只执行指定或已获授权的检查、构建、测试或运行；请求不包含修复时，失败后只报告。
- 元任务：维护本 skill 或处理不依赖 IDEA 项目语义的普通文档。元任务只决定工具边界，不排斥其同时属于分析型、方案型、修改型或验证型。

意图能由请求和上下文唯一确定时直接继续；只有不同理解会实质改变范围、行为或结果时才询问。

## 项目证据与工具边界

涉及项目源码、符号、依赖、工作区、VCS、编辑、inspection、运行配置、构建、数据库或调试能力时，使用 IDEA MCP 取得当前项目证据。

需要跨文件定位、调用关系或影响候选，且 CodeGraph 可用并适合当前查询时，使用 CodeGraph 辅助定位；关键结论仍由 IDEA MCP
读取的当前项目内容复核。已有证据足够、CodeGraph 不可用或无结果时，不继续尝试，也不自行初始化、重建或修复索引。

仅维护本 skill、处理普通文档或执行不依赖 IDEA 项目语义的元任务时，可以使用普通文件系统工具。

按任务确认项目根、模块和目标文件归属。目标受 VCS 管理时，修改前读取相关仓库的已暂存、未暂存、未跟踪及冲突状态；目标不受 VCS
管理时，不假设存在 Git 仓库。

- Java、Kotlin 及其他具有可靠语义模型的源码，优先使用符号、定义和调用分析。
- Vue、TypeScript、模板、样式和配置先按精确文件名、标识符、固定文本或正则定位，再读取当前内容。
- 前后端契约以 HTTP method 和 URL 串联 Controller、请求响应类型、API 封装和页面调用。
- 依赖、JDK、JAR、JRT 或反编译源码由 IDEA 读取并核对当前版本。
- 公共符号重命名使用语义重构，不用文本全局替换模拟。

IDEA MCP、CodeGraph 或任务必需工具不可用时，不伪造已经读取或验证的证据；说明缺失能力、受影响范围和仍能完成的部分。

## IDEA MCP 子工具命令

IDEA 子工具统一通过 `mcp__idea__execute_tool` 调用。已知任务项目时，外层传入其绝对 `projectPath`；子工具名称和参数放入
`command`。

首次使用某类子工具前，完整读取对应契约：

- 搜索、读取、符号、目录、编辑器状态和 VCS：读 [IDEA-MCP上下文工具契约.md](references/IDEA-MCP上下文工具契约.md)。
- 创建文件、格式化、重命名、inspection 和构建：读 [IDEA-MCP编辑与验证工具契约.md](references/IDEA-MCP编辑与验证工具契约.md)。
- 数据源、schema、数据库对象、查询和结果：读 [IDEA-MCP数据库工具契约.md](references/IDEA-MCP数据库工具契约.md)。
- 运行配置及运行任务：读 [IDEA-MCP运行工具契约.md](references/IDEA-MCP运行工具契约.md)。

一项任务使用多类工具时，读取全部对应文件。四份契约列出本 skill 允许调用的 33 个子工具及已确认参数；不得使用清单外工具或根据相邻工具推断名称和参数。

`command`
按统一入口要求传入包含子工具名和参数的命令行字符串。只有当前契约或同一工具、同一参数结构的有效既有命令能够唯一确定序列化方式时才构造命令。可选参数的单位、合法值、路径基准或序列化规则未查明时省略该参数；必填参数仍无法唯一构造时，报告契约缺口并停止，不试探写法。

构造和判读命令时：

1. 明确命令目的、目标对象和预期证据。
2. 从对应契约复制工具名、参数名、必填项、已确认类型和条件组合。
3. 核对外层项目、路径基准、字符串边界、数组、对象、布尔值、数字和互斥组合。
4. 同时检查统一入口和实际子工具结果；外层调用成功不代表子工具成功。
5. 输入、配置和外部状态未变化时，不为取得不同结果盲目重跑。

首次准备执行终端命令、脚本、包管理器或包装入口、编译、构建、测试，或者处理高输出、失败、超时和结果丢失时，读取 [命令执行可靠性.md](references/命令执行可靠性.md)
。成功的简单 IDEA 搜索、读取和状态查询不要求加载该文件。

## 任务路由

下列条件独立判断。同一任务命中多项时，加载全部命中项；标明“追加”的文件不得替代其基础文件。

- 源码理解、定位、调用与影响分析、Bug 排查或准备修改：读 [源码与上下文阅读.md](references/源码与上下文阅读.md)。
- 修改或新增项目文件，以及处理本次变更文件的格式化、IDEA inspection
  和文件级只读检查：读 [编辑格式化与文件级检查.md](references/编辑格式化与文件级检查.md)。
- 数据库、SQL、持久化层、ORM、Mapper、DAO、Repository、字段、字典、权限数据、数据异常、DDL 或
  DML：读 [数据库SQL与持久化层.md](references/数据库SQL与持久化层.md)。
-
用户要求先给方案、进行方案比选、输出设计或计划正文，或继续调整和确认已输出方案：读 [方案比选与确认.md](references/方案比选与确认.md)。
- 进入系统功能设计或系统详细设计正文：追加 [系统功能详细设计格式.md](references/系统功能详细设计格式.md)。
- 进入工程开发计划正文：追加 [工程开发计划格式.md](references/工程开发计划格式.md)。
- 进入文档、规范、说明性配置或 skill 的修改方案正文：追加 [文档修改方案格式.md](references/文档修改方案格式.md)。
- 前端任务：读 [前端开发工作流.md](references/前端开发工作流.md)；Vue 页面、组件、指令、composable、store 或 Vue
  工具链任务追加 [Vue开发工作流.md](references/Vue开发工作流.md)
  ；新界面、视觉重构、样式美化或体验提升追加 [前端界面设计质量.md](references/前端界面设计质量.md)。
- 目录、模块、项目、工作区或运行环境级
  inspection、lint、类型检查、编译、构建、测试、启动、浏览器、截图、长任务、独立验证或运行依赖问题：读 [非文件级执行与验证.md](references/非文件级执行与验证.md)。
- 删除非数据库文件或版本历史、不可逆操作、依赖或环境变更、持久化 IDE 配置、部署、发布、Git commit、Git push 或
  CI/CD：读 [高风险确认.md](references/高风险确认.md)。

## 事实、决策与授权

- 技术事实：从项目规范、源码、配置、文档、依赖或数据库只读信息查明。
- 待定决策：必要查证后仍不能唯一确定，且不同选择会改变业务行为、范围、契约、数据、安全或结果。
- 操作授权：查明实际入口、工作目录、目标范围、展开行为和主要影响后，判断用户当前明确指令是否覆盖该操作。

发现待定决策或未解决授权时：

1. 继续完成解决该事项所需的最小只读查证，以及与该事项完全独立且不会影响其结果的工作。
2. 暂停依赖该事项的编辑、执行和验证。
3. 使用稳定的 `Q1`、`Q2` 编号说明已知事实、未知内容、影响和范围；适合时提供 A/B/C 与建议。
4. 提问后结束当前轮。

文件级默认授权和检查闭环由 [编辑格式化与文件级检查.md](references/编辑格式化与文件级检查.md)
负责；文件范围之外的执行由 [非文件级执行与验证.md](references/非文件级执行与验证.md)
负责；数据库写入由 [数据库SQL与持久化层.md](references/数据库SQL与持久化层.md)
负责；其他高风险操作由 [高风险确认.md](references/高风险确认.md) 负责。

## 确认状态

每项只处于未确认、已确认或已失效状态。回复能够唯一确定选择和范围时才确认；用户拒绝、取消相关工作或范围变化使事项不再影响后续时，记为已失效并记录原因。

- 单个、范围唯一的问题，可以用能够唯一对应的自然语言肯定或否定确认。
- 多个问题时，“继续”“开始”“可以”“你看着办”等泛化回复不覆盖未逐项对应的事项。
- 部分回复只更新对应事项；保留其他未确认项，不重复询问已确认项。
- 推荐、沉默和超时不是默认答案。
- 用户明确表示全部按已列出的推荐项及范围处理，且各项唯一清晰时，可以确认对应事项。
- 新回复改变选择、范围或产生分支时，以最新明确指令为准并重新检查受影响工作。

依赖某项确认的编辑、执行或验证，只能在该项已确认或已失效后进行；解决该项所需的最小只读查证不受此限制。

## 标准闭环

1. 审查任务前提、信息完整性和证据状态，确定任务类型、范围、风险及全部适用 reference。
2. 读取适用的 `AGENTS.md`、项目文档、当前工作区状态和任务上下文。
3. 使用任务所需的 IDEA MCP，并按需使用 CodeGraph，取得当前项目证据。
4. 核实事实、检验推断并查明实际影响。
5. 解决影响结果的待定决策和操作授权；方案任务按 [方案比选与确认.md](references/方案比选与确认.md) 完成正文。
6. 在已确认范围内小步编辑，并按 [编辑格式化与文件级检查.md](references/编辑格式化与文件级检查.md) 完成本次变更文件的闭环。
7. 只修复本次新增或直接相关的问题，按授权执行必要的非文件级验证。
8. 复核结果完整性，按真实终态交付。

同一对话复用已经完整掌握且未变化的 reference、项目规范、文件内容和分析结论；只在内容变化、首次读取失败或不完整、上下文未保留必要正文、证据冲突或用户明确要求时补读缺失范围。

## 结果终态

- 已完成：请求范围完成，所有强制验证取得完整结果。
- 已完成但未充分验证：实现完成，非强制验证因授权、环境或能力限制未完成；明确说明限制，不宣称通过。
- 阻塞：待定事项、必需工具、强制验证或高风险授权未解决；说明阻塞并请求必要输入。

准确说明已完成内容、验证范围、未验证项和阻塞。方案结果按 [方案比选与确认.md](references/方案比选与确认.md) 及对应正文
reference 交付，不自动进入其他正文或实施。
